{
  "name": "vercel-env-analyzer",
  "version": "1.0.0",
  "description": "Chrome extension to analyze Vercel environment variables",
  "scripts": {
    "build": "npm run build:clean && npm run build:typescript && npm run build:tailwind && npm run build:copy",
    "build:clean": "rimraf dist",
    "build:typescript": "tsc",
    "build:tailwind": "tailwindcss -i ./src/styles.css -o ./dist/styles.css",
    "build:copy": "copyfiles -u 1 src/*.html src/manifest.json dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.237",
    "copyfiles": "^2.4.1",
    "rimraf": "^3.0.2",
    "tailwindcss": "^3.3.0",
    "typescript": "^5.0.0"
  }
}